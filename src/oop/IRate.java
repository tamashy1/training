package oop;

public interface IRate {
//create outline but leave it to user
	//point of interface is to make things reusable
	//Write the class outline
	public void setRate();
	public void increaseRate();
}
